from setuptools import setup, find_packages

setup(
    name='pyask',
    version='0.0.1',
    description='Easily build responses for the '
                'Alexa Skills Kit (ASK) Interface',
    author='Brian Nuszkowski',
    author_email='nuszkowski@protonmail.com',
    packages=find_packages(),
    url='https://github.com/broamski/pyask'
)
